<?php

/**
 * @file
 * Declares a bogus encryption method and key provider for testing purposes.
 */

/**
 * Implements hook_plugin_manager_directory().
 */
function encrypt_test_plugin_manager_directory($module, $plugin) {
  if ($module == 'encrypt') {
    return 'plugins/' . $plugin;
  }
}
